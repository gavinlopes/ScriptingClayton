proc float AvgNum(float $num1, float $num2)
{
	float $sum[];
	//Have to add numbers then divide by amount.
	
	return $avg;
	//return the average for mean
}

AvgNums ({ 4, 3, 8, 9, 14, 12, 0, 3, 75 });
// FindAvg of list of numbers.